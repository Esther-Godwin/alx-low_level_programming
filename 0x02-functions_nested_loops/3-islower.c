/**
* _islower - Checks if an alphabet is lower or upper case
* @c: The alphabet to check
*
* Return: If the alphabet is lovercase return 1, otherwise return 0
*/
int _islower(int c)
{
	if (c > 90)
		return (1);
	else
		return (0);
}
