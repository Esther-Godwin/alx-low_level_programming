/**
* _abs - Computes the absoiute value of an integer
* @n: Integer to find the absolute value of
*
* Return: The absolute value of the integer
*/
int _abs(int n)
{
	if (n < 0)
		return (n * -1);
	return (n);
}
